[tool.poetry]
name = "aixblock_core"
version = "0.0.8"
description = ""
authors = ["Tony Shark <tony@aixblock.io>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
aixblock-core = 'aixblock_core.manage:server'