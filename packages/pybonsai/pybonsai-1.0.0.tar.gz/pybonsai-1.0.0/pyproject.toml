[project]
name = "pybonsai"
version = "1.0.0"
description = "A project that generates ASCII art trees in your terminal."
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.9"
keywords = ["pybonsai"]
authors = [
    {name = "Ivan Karlov", email = "karlovivan679@gmail.com"}
]
classifiers = [
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]

[project.scripts]
pybonsai = "pybonsai.__main__:main"

[project.urls]
"Source" = "https://github.com/DaEtoJostka/PyBonsai"
"Original" = "https://github.com/Ben-Edwards44/PyBonsai"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["pybonsai"]
