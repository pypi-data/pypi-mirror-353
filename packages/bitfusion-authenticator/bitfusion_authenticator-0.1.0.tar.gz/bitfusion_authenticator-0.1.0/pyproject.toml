[project]
name = "bitfusion-authenticator"
version = "0.1.0"
description = "JWT validation with FastAPI integration for microservice auth"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [
    {name = "Gleb", email = "gleb@bitfusion.trade"}
]
dependencies = [
    "python-jose[cryptography]",
    "fastapi",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
