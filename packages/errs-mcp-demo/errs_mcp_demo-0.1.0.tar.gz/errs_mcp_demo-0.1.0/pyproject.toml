[project]
name = "errs-mcp-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "exid", email = "exid310l@outlook.com" }
]
requires-python = ">=3.12"
dependencies = [
    "map[cli]>=1.3.0",
]

[project.scripts]
errs-mcp-demo = "errs_mcp_demo:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
