# {{ project_name }}

**Generated by ChimeraStack v{{ cli_version }}**

## 🚀 Quick Start

1. **Start the environment**:
   ```bash
   docker-compose up -d
   ```

2. **Stop the environment**:
   ```bash
   docker-compose down
   ```

## 📋 Available Services

| Service | URL |
|---------|-----|
{% for service, port in ports.items() %}
| {{ service }} | http://localhost:{{ port }} |
{% endfor %}

## 💻 Development

{% if db_variant %}
### Database Information
- **Type**: {{ db_variant }}
- **Host**: {{ db_variant }}
- **Port**: {{ ports.db }}
- **Database**: {{ project_name }}
- **Username**: {{ project_name }}
- **Password**: secret
{% endif %}

{% if "frontend" in services %}
### Frontend
- The React frontend is located in the `frontend/` directory.
- Run `cd frontend && npm start` for hot-reload development.
{% endif %}

{% if "php" in services or "web" in services %}
### Backend
- PHP files should be placed in the `src/` directory.
- The web server is configured to serve from the `public/` directory.
{% endif %}

## 📁 Project Structure

- `/src` - Source code
- `/public` - Publicly accessible files
{% if "frontend" in services %}
- `/frontend` - React frontend application
{% endif %}
- `/docker` - Docker configuration files

## 🔧 Customization

Edit the `.env` file to modify environment variables, including port mappings.
