from setuptools import setup, find_packages

setup(
    name='coher',  # Change this to your library name
    version='0.1.0',
    description='A Python library by Unknown',
    author='Unknown',
    author_email='123@email.com',
    packages=find_packages(),
    install_requires=[],  # List dependencies here if any
    python_requires='>=3.6',
)
