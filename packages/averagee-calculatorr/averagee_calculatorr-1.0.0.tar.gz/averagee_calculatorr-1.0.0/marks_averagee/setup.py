from setuptools import setup, find_packages

setup(
    name="averagee_calculatorr",
    version="1.0.0",
    description="A library to calculate the average of marks",
    author="Your Name",
    packages=find_packages(),
    install_requires=[],
    python_requires=">=3.6",
)