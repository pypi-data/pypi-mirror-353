[project]
name = "hero-api-service"
version = "0.1.3"
description = "Transform services in default endpoints integrated by FastAPI and Pydantic."
readme = "README.md"
requires-python = ">=3.10.0"
dependencies = [
    "fastapi>=0.115.12",
    "pydantic>=2.11.5",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
