[project]
name = "aminulhoque"
version = "0.1.0"
description = "Encrypt and decrypt audio files as text for secure voice messaging."
authors = [{name = "Aminul Hoque", email = "aminul98547@gmail.com"}]
readme = "README.md"
requires-python = ">=3.7"
dependencies = [
    "cryptography>=41.0.0"
]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta" 