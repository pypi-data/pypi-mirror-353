[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "hello-bye-hello-pypi"               # Use dashes here (PyPI standard)
version = "0.0.1"
description = "A short description of your package."
readme = "README.md"
license = { file = "LICENSE" }
authors = [
  { name = "Your Name", email = "your@email.com" }
]
requires-python = ">=3.7"
dependencies = []

[project.urls]
Homepage = "https://github.com/yourusername/hello-bye-hello-pypi"
