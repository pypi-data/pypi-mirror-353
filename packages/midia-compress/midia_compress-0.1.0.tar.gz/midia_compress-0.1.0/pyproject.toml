[project]
authors = [
  {name = "Gustavo Rizzo S M de Albuquerque", email = "grizzo.albu@gmail.com"},
]
dependencies = ["typer (>=0.16.0,<0.17.0)"]
description = ""
name = "midia-compress"
readme = "README.md"
requires-python = ">=3.13,<4.0"
version = "0.1.0"

[project.scripts]
midia-compress = "midia_compress.cli:app"

[tool.poetry]
packages = [{include = "midia_compress", from = "src"}]

[build-system]
build-backend = "poetry.core.masonry.api"
requires = ["poetry-core>=2.0.0,<3.0.0"]
