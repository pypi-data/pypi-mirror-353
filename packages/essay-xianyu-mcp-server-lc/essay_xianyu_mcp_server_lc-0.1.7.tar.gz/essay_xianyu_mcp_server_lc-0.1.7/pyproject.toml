[project]
name = "essay-xianyu-mcp-server-lc"
version = "0.1.7"
description = "获取闲鱼相关信息MCP服务"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli,httpx]>=1.9.0",
]

[project.scripts]
essay-xianyu-mcp-server-lc = "essay_xianyu_mcp_server_lc:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
