[project]
name = "logpunch"
version = "1.0.0"
description = "Logpunch is a lightweight Python package providing a customizable colored logger and enhanced exception handling with detailed error tracking — designed to streamline logging and debugging in data science and machine learning projects."
readme = "README.md"
requires-python = ">=3.7"
license = {text = "MIT"}
authors = [
  { name = "Gourav Gulia", email = "grvgulia007@gmail.com" }
]
dependencies = [
  "pydantic",
  "colorama"
]

keywords = ["logging", "logger", "exception", "data-science", "machine-learning"]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"