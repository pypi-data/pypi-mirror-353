from setuptools import setup, find_packages

setup(
    name="area_calculator_mindbox_testtask",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[],
    python_requires=">=3.6",
)
