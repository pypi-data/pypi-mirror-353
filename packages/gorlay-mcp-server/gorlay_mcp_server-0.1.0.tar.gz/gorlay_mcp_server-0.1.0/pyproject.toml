[project]
name = "gorlay-mcp-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]",
]

[project.scripts]
gorlay-mcp-server = "gorlay_mcp_server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
