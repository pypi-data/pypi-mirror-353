[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "dphysicsengine"
version = "0.1.8"
description = "Motor físico para simulaciones"
authors = [
    {name = "David García Díaz", email = "davidgrcdiaz@gmail.com"}
]
requires-python = ">=3.7"
dependencies = ["pillow"]

[tool.setuptools.packages.find]
where = ["./physics"]
