[project]
name = "elif"
version = "0.1.0"
description = ""
authors = [
    {name = "Supertelligence", email = "pypi@supertelligenceai.com"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
]

[tool.poetry]
packages = [{include = "elif", from = "src"}]

[tool.poetry.scripts]
elif = "elif.main:main"


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
