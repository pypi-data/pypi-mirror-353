"""
The catalog subpackage for the opatio library.

This module handles the catalog or index entries that map index vectors to the
byte location and metadata of data cards within the OPAT file. 

-------
entry
    Defines the CardCatalogEntry class for representing entries in the
    OPAT file's main catalog.


"""