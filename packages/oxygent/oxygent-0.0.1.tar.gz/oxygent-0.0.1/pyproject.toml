[tool.poetry]
name = "oxygent"
version = "0.0.1"
description = "Multi-Agent Framework"
authors = ["jd-oxygen <litianlong26@jd.com>"]

[tool.poetry.dependencies]
python = "^3.10"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
