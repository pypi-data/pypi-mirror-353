[project]
name = "lzhfreshrssapi"
version = "0.2.0"
description = "获取`FreshRSS`数据库中对应`url`的订阅源中已有的`guid`的集合"
authors = [
    { name = "lzh", email = "zhhtdm@hotmail.com" }
]
license = { text = "MIT" }
readme = "README.md"
requires-python = ">=3.7"
dependencies = [
    "asyncpg",
    "aiohttp"
]

[project.urls]
Homepage = "https://github.com/zhhtdm/freshrss-api"
Repository = "https://github.com/zhhtdm/freshrss-api"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
