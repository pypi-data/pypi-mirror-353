[project]
name = "cemcp"
version = "0.0.2"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=2.7.0",
]
[tool.hatch.build]
packages = ["src"]

[tool.hatch.build.targets.wheel]
packages = ["src/cemcp"]

[project.scripts]
cemcp = "cemcp.cemcp:main"