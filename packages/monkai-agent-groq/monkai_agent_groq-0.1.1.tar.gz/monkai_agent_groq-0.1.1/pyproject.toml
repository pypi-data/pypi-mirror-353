[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "monkai-agent-groq"
version = "0.1.1"
description = "Groq integration for MonkAI Agent"
requires-python = ">=3.8"
dependencies = [
    "monkai-agent>=0.1.1",
    "groq"
]

[tool.hatch.build]
packages = ["monkai_agent"]
namespace = "monkai_agent.groq"
