[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "evalz"                                              # your desired PyPI name
version = "0.1.0"
description = "Evaluations!"
authors = [{ name = "Andrew", email = "andrew@wandb.com" }]
readme = "README.md"
requires-python = ">=3.7"
license = { text = "MIT" }
