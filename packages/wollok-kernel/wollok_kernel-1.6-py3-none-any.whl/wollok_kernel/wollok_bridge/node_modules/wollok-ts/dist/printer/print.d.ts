import { Node } from '../model';
type PrintSettings = {
    maxWidth: number;
    useSpaces: boolean;
    abbreviateAssignments: boolean;
};
export default print;
declare function print(node: Node, { maxWidth, useSpaces, abbreviateAssignments }?: PrintSettings): string;
