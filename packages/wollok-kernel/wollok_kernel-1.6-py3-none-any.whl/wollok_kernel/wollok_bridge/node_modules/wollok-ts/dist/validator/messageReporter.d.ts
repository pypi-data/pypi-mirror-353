export declare enum LANGUAGES {
    SPANISH = "es",
    ENGLISH = "en"
}
export type Message = {
    [key: string]: string;
};
export type Messages = {
    [key in LANGUAGES]: Message;
};
export type ReportMessage = {
    message: string;
    values?: string[];
    language?: LANGUAGES;
    customMessages?: Messages;
};
export declare const getMessage: ({ message, values, language, customMessages }: ReportMessage) => string;
