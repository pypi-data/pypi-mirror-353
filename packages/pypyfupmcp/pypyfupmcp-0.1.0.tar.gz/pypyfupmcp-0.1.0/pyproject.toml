[project]
name = "pypyfupmcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "datafu", email = "24384760+datafu@users.noreply.github.com" }
]
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.9.3",
]

[project.scripts]
pypyfupmcp = "pypyfupmcp:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
