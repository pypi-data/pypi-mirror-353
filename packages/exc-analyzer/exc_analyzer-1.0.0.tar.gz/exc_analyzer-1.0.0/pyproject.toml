[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "exc-analyzer"
version = "1.0.0"
description = "GitHub repo & user analyzer with secret scanner"
readme = "README.md"
requires-python = ">=3.7"
license = {text = "MIT"}
authors = [
    {name = "Berat", email = "brgkdm@proton.me"}
]
dependencies = ["requests"]

[project.scripts]
exc = "exc_analyzer:main"