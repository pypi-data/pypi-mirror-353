[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "observee"
version = "0.1.8"
authors = [
    { name = "Observee Team" },
]
description = "Python SDK for tool usage logging and monitoring"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "httpx",
    "pydantic",
]

[project.urls]
"Homepage" = "https://github.com/observee-ai/python-sdk"
"Bug Tracker" = "https://github.com/observee-ai/python-sdk/issues" 