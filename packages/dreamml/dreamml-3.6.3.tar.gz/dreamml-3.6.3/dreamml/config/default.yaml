defaults:
  - _self_
  - data: default
  - models: default
  - pipeline: default
  - stages: default
  - validation: default

# Служебные фичи для удаления, имя для группировки nlp семплов также написано в fitter
_service_fields: ["_group_nlp_aug_field"]

# путь к папке с конфигом, отличающийся от стандартного
config_dir: