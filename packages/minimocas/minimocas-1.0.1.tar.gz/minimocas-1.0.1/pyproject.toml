[build-system]
requires = ["setuptools >= 75.3.2"]
build-backend = "setuptools.build_meta"

[project]
name = "minimocas"
version = "1.0.1"
authors = [
  { name="Roman", email="author@example.com" },
]
description = "minimocas companion package"
readme = "README.md"
requires-python = ">=3.8"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]
license = { text="MIT" }
