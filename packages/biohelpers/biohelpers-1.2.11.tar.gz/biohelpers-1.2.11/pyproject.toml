[build-system]
requires = ["setuptools>=61.0.0", "setuptools-scm>=6.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
# 版本号格式：从Git标签提取（如v1.0.0）
version_scheme = "no-guess-dev"
local_scheme = "no-local-version"
