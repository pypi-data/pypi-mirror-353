[project]
name = "labyr"
version = "0.2.0"
description = ""
authors = [
    {name = "sigmacodeslol"}
]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
]


[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
