[project]
name = "gigo-py"
version = "0.0.0"
description = ""
readme = { file = "README.md", content-type = "text/markdown" }
requires-python = ">=3.11"
dependencies = []
authors = [{ name = "Yuda", email = "yuda03979@gmail.com" }]
license = { text = "MIT" }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["gigo"]